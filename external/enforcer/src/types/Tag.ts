import { TagShared } from "../../../../lib/types/shared/TagShared";

export namespace Tag {

    export type PartialView = TagShared;

    export type VerboseView = TagShared;

    export type FullView = TagShared;
}