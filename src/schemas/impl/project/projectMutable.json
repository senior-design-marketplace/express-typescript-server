{
    "title": "ProjectMutable",
	"description": "An effort made by some group of people to achieve something in the Marqetplace",
	"type": "object",
	"properties": {
		"title": {
			"description": "The title of the project",
			"type": "string",
			"minLength": 1,
			"maxLength": 256
        },
        "tagline": {
            "description": "A short description of the project",
            "type": "string",
            "minLength": 1,
            "maxLength": 256
        },
		"body": {
			"description": "The body of the description",
			"type": "string",
			"minLength": 1,
			"maxLength": 2048
        },
		"tags": {
			"description": "Tags associated with the project",
			"type": "array",
			"minItems": 0,
			"items": {
				"$ref": "../tag.json"
			}
        },
        "requestedMajors": {
            "description": "Majors of students that are needed for the project",
            "type": "array",
            "minItems": 0,
			"items": {
				"$ref": "../major.json"
			}
		},
		"acceptingApplications": {
			"description": "A flag set by the advisor to prevent or enable further applications",
			"type": "boolean"
        }
    }
}
