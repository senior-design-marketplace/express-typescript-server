{
    "title": "Tag",
    "description": "A tag attached to a project denoting some interest.  All majors are tags by default",
    "anyOf": [
        {
            "$ref": "major.json#"
        },
        {
            "enum": [
                "Programming",
                "Artificial Intelligence"
            ]
        }
    ]
}
