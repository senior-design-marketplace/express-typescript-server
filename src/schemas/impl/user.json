{
	"title": "User",
	"description": "A person who is using the Senior Design Marketplace system",
	"type": "object",
	"properties": {
		"id": {
			"description": "The identity of the user",
			"type": "string"
		},
		"firstName": {
			"description": "The first name of the user",
			"type": "string",
			"minLength": 1,
			"maxLength": 256
		},
		"lastName": {
			"description": "The last name of the user",
			"type": "string",
			"minLength": 1,
			"maxLength": 256
		},
		"email": {
			"description": "The user's email address",
			"type": "string",
			"format": "email"
		},
		"avatarLink": {
			"description": "A link to a small photo of the user",
			"type": "string"
		}
	},
	"required": ["id"]
}
